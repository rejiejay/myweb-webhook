const index = {
	data: {
		namespace: 'index',

		state: {
		},

		reducers: {}
	},

	init: function (app) {
		// app._store.dispatch({
		// 	type: 'index/reducers',
		// 	data: ''
		// });
	}
}

export default index;
